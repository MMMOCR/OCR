#include "job.h"
#include <stdlib.h>

int
job(neural_network nn, char *inputs)
{
    double input_layer[INPUT_COUNT];
    double output_layer[OUTPUT_COUNT];
    double *hidden_layer = malloc(nn.sizes.hidden_count * sizeof(double));



    return 0;
}
